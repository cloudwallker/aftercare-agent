# 实施进度

依据：`docs/superpowers/plans/2026-09-18-aftercare-agent-implementation.md`。
用户已于 2026-09-18 授权开始实现；原设计审阅稿保留作为基线。

1. 工程、数据约束与角色隔离：已完成；代码审查、宿主与 Docker 内真实 PostgreSQL 测试通过。
2. 模拟商家 HTTP 幂等账本：已完成；标准 Docker 全套 80 项测试通过，含真实 TCP 商家进程重启。
3. 持久任务、租约与检查点：已完成；Docker 全套 110 项测试通过，API 健康启动及代码审查通过。
4. 只读 Agent 与待审方案：已完成；Docker 全套 163 项测试及三条独立 API/Worker/商家调查链路通过。
5. 审批与重新调查：已完成；幂等决定、并发竞争、重新调查与过期扫描测试通过。
6. 退款执行与对账：已完成；Docker 全套 202 项测试通过，含两个真实 Worker 强杀恢复窗口及标准 Compose 完整审批退款链路。
7. API、SSE 与故障演示：已完成；真实 TCP 心跳/续传、三条演示 CLI、干净 Compose 部署通过。
8. HTTP Provider、固定评测与交付：离线版已完成；236 项测试通过，Mock 固定样本 20/20；真实模型端到端未验证。

开发阶段未初始化 Git；2026-09-20 用户授权整理发布、初始化 Git 并上传 GitHub，提交使用 GitHub 隐私邮箱。
初始环境：Docker 引擎未运行；uv 默认缓存/解释器目录不可访问，改用项目内 `.cache`。

## 2026-09-18 验证记录

- Python 3.12.12 环境及 `uv.lock` 已建立。
- 空测试库 smoke test 因缺少 `public.alembic_version` 失败，随后升级 `0001_initial` 成功，重复升级成功。
- 宿主 Python 连接 Docker PostgreSQL 16：27 项单元测试 + 16 项集成测试，共 43 项通过。
- Ruff、Python 编译检查和离线锁文件一致性检查通过。
- 代码审查发现 Token 首尾空白导致判重失效；新增回归先失败再修复，复审通过。
- 数据库测试发现 SQLAlchemy 将内嵌 JSON 冒号误判为参数；改为类型化参数绑定后通过。
- 测试使用独立 `aftercare-test` project 和 `aftercare_test` 库，没有开发库清表或 Git 提交。
- 尚未实现任务 2～8，不将基础测试结果视作 A01～A22 全部验收。
- 生产与测试 Docker 镜像构建成功；标准 Compose 运行 43 passed in 0.71s，数据库恢复为无宿主端口映射。

## 2026-09-20 任务 2

- 已实现商家订单/物流查询、Bearer 鉴权、幂等退款 POST、原键 GET 查询与健康检查。
- 相同 key 同参优先重放；不同 key 通过订单行锁和唯一索引防重，确定拒绝持久化 declined。
- `commit_then_503_once` 只允许测试组合根启用；fault_consumed 和账本同事务写入，进程重启后可查询/重放原退款。
- seed 只插入缺失订单，初始已退款样例与账本同事务；并发及真实退款后重跑均不覆盖历史。
- 宿主首轮 78 项通过；新增真实 TCP 重启与斜杠 key 回归后，Docker 全套 80 passed in 4.68s。
- 代码审查发现斜杠 key 的查询路由缺陷；回归确认 POST 200/GET 404 后改 `{key:path}`，测试及复审通过。
- Windows 测试子进程显式使用 SelectorEventLoop，以兼容 psycopg；标准生产 Compose 运行 Linux。
- 下一项：任务 3 持久任务、命令幂等、租约与检查点。任务 3～8 尚未实现。
- 标准 Compose seed 两次分别新增 6/0 个订单，商家容器健康启动成功。本次测试服务结束后关闭，数据卷保留。

## 2026-09-20 任务 3

- 已实现创建命令幂等事务、Bearer 身份与可见性校验、任务详情及任务/步骤游标分页。
- 已实现 SKIP LOCKED 领取、数据库时钟、租约续租与 epoch 写入校验；过期未接管也拒绝续租和写回。
- 步骤预扣预算、pending 接管、成功结果复用、checkpoint 和事件原子提交已实现；覆盖重试次数、输出大小、本代时间及无效输出次数限制。
- Worker 提供可测试分派入口，包含领取前并发槽位、独立心跳、失租取消和有界停机。尚未接入调查 Agent，不会伪报业务成功。
- 宿主阶段回归 107 passed in 16.22s；补充边界测试后标准 Docker 全套 110 passed in 9.16s（37 单元、72 集成、1 TCP 端到端）。Ruff、编译和代码审查通过。
- Compose API 健康启动成功；真实 HTTP `/health/ready` 返回 200，OpenAPI 包含任务创建、查询和步骤接口。
- 运行时契约见 [runtime.md](runtime.md)，验收细节见 [test-results.md](test-results.md)。下一项为任务 4 只读 Agent 与待审方案；任务 4～8 待实现。

## 2026-09-20 任务 4

- 实现 ModelProvider 契约、MockProvider、三类只读工具、严格商家快照及 refund_v1 规则校验；模型不能覆盖订单、客户、金额或执行权限。
- 调查循环复用持久化动作和成功步骤，保持 pending 尝试数及本代预算；非法输出最多一次纠正，计数与模型步骤同事务提交。
- 校验证据归属、代次、最新成功工具结果及一致版本；生成规范哈希和 24 小时有效的不可变审批方案，与 waiting_approval 和事件原子提交。
- 已启用独立 Worker 入口及 Compose 服务；默认离线 Mock，HTTP Provider 等待任务 8，审批决定与退款执行等待任务 5、6。
- Docker 最终回归 163 passed in 24.33s（65 单元、97 集成、1 商家 TCP 端到端），Ruff 和编译检查通过。
- 独立 API/Worker/商家真实 HTTP 验证合格退款待审批、规则拒绝、纯查询 no_action；对应三笔订单无新增退款记录。测试使用随机前缀的独立模拟数据，保留历史。
- 下一项：任务 5 审批决定、版本失效与重新调查。任务 5～8 待实现，真实模型和 Worker 强杀恢复尚未验证。

## 2026-09-20 任务 5、6

- 新增审批决定、审批历史与重新调查 API；角色、可见性、代次、哈希和期限校验均在命令事务中执行。20 个同键决定原样重放，不同键或相反决定竞争只有一个成功。
- 重新调查失效旧审批，重置本代预算/检查点并增加 generation、epoch；Worker 每五秒扫描未冻结的过期授权，running 和已冻结操作不被扫描抢写。
- 退款冻结请求及原键后先 GET，missing 才同键同参 POST；调用前持久化计数与 unknown，响应严格匹配原请求，结果与任务/事件同事务提交。
- 三次 POST、八次 GET、120 秒总期限及持久退避已实现；不确定结果或冻结后程序异常进入 manual_review，保留订单占用。POST 额度耗尽后继续 GET，不丢失最后一次请求可能已经成功的结果。
- 两个工单争同一订单、版本变化、授权到期、事务失败、旧 epoch 外部返回与程序异常测试通过。真实 Worker 在 HTTP 前及商家成功后本地提交前被强杀，新 Worker 按真实租约接管，原 key 不变且各仅一条商家 confirmed。
- 首轮 185 passed in 21.49s；补齐边界与强杀测试后 202 passed in 31.93s（74 单元、125 集成、3 TCP/进程端到端）。Ruff、编译检查通过。
- 下一项为任务 7 API/SSE 与完整演示脚本；任务 8 的真实模型适配器与评测也仍待实现。
- 标准 Compose 启动 API、商家与 Worker 后，真实 HTTP 创建→调查→批准→退款成功，商家仅一条 confirmed，任务终态后审批命令仍原样重放；随机前缀模拟订单与测试历史保留。

## 2026-09-20 任务 7、8

- SSE 身份校验、high-water 分批补读、Last-Event-ID、15 秒心跳与终态关闭完成，真实 TCP 验证通过。
- HTTP Provider 关闭并行调用，恢复持久化工具消息配对，错误和重试由统一 runtime 控制；契约及完整调查集成测试通过。
- 调查步骤完成后真实强杀恢复通过；三条独立演示 CLI、只读 inspect-refund、全新数据卷部署与 6/0 重复 seed 通过。
- 最终 Docker 回归 236 passed in 63.62s；固定 Mock 20 条全部匹配、8 条违规建议拦截、重复退款 0。实际详情见 test-results.md。
- 任务 1～8 离线 MVP 已完成。没有真实模型配置，因此未调用外部模型，不将 Mock 匹配率当作真实业务能力；来源与许可证边界见 references.md。
